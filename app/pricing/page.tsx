import Link from 'next/link'

const plans = [
  { name: 'Free', price: '₹0', note: 'For getting your career system started.', features: ['Resume import + basic audit', 'Resume builder', 'Basic ATS checks', 'Job discovery + saved roles', 'Career direction snapshot', 'Limited AI credits'], cta: 'Start free' },
  { name: 'Pro', price: '₹499', note: 'For an active job search.', features: ['Everything in Free', 'Unlimited resume versions', 'Deep ATS + role-fit analysis', 'Job-post intelligence', 'Tailored resume + cover letter', 'Career paths + salary target planning', 'Interview preparation', 'Application automation credits', 'Advanced tracking + analytics'], cta: 'Choose Pro' },
]

export default function Pricing(){ return <main className="main standalone"><Link className="back" href="/">← Command</Link><div className="mono eyebrow">WAYO / PRICING</div><div className="careerHead"><div><h1 className="title">Pay for leverage, not access.</h1><p className="lead">WAYO stays useful for free. Pro unlocks deeper intelligence, higher AI limits and controlled application automation.</p></div><div className="targetBox"><div><span>MODEL</span><strong>Freemium</strong></div></div></div><section className="careerRoles">{plans.map(p=><article className="card careerRole" key={p.name}><div className="roleLine"><div><div className="mono">{p.name.toUpperCase()}</div><h2>{p.price}<span className="small"> / month</span></h2></div>{p.name==='Pro'&&<span className="badge">RECOMMENDED</span>}</div><p className="small">{p.note}</p><div className="chips">{p.features.map(f=><span className="chip" key={f}>{f}</span>)}</div><button className={p.name==='Pro'?'primary':'ghost'}>{p.cta}</button></article>)}</section><section className="section card"><div className="sectionhead"><h3>Future monetisation layers</h3><span className="mono">ROADMAP</span></div><div className="chips"><span className="chip">Extra automation credits</span><span className="chip">Career intelligence packs</span><span className="chip">Interview practice packs</span><span className="chip">Premium data connectors</span><span className="chip">Community Pro</span><span className="chip">Team / coach plans</span></div><p className="small">Pricing is provisional until payment infrastructure and unit economics are validated.</p></section></main> }
