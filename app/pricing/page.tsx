import Link from 'next/link'
import { Check } from 'lucide-react'
import { BRAND } from '@/lib/brand'
import { Surface, SectionHeader, KindleapShell, Status } from '@/components/kindleap-ui'
const plans=[{name:'Free',price:'₹0',features:['Basic resume tools','Job discovery','Saved roles','Basic fit signals'],href:'/opportunities'},{name:'Pro',price:'₹499/month',features:['Resume intelligence','Application studio','Career planning','Controlled automation'],href:'/auth?mode=sign-up'}]
export default function Pricing(){return <KindleapShell><div className="kl-page"><SectionHeader eyebrow={`${BRAND.name} / Access`} title="Simple access. Useful tools." description="The free layer covers discovery. Pro is intended for deeper preparation and controlled execution."/><div className="kl-card-grid">{plans.map(p=><Surface key={p.name}><div className="kl-row-head"><div><div className="kl-eyebrow">{p.name}</div><h2>{p.price}</h2></div>{p.name==='Pro'&&<Status>Pro</Status>}</div><div className="kl-checks">{p.features.map(f=><div key={f}><Check size={14}/>{f}</div>)}</div><Link className={`kl-button ${p.name==='Pro'?'kl-button-primary':''}`} href={p.href}>{p.name==='Pro'?'Create account':'Explore'}</Link></Surface>)}</div><footer className="kl-page-footer">Payment lifecycle is not yet connected. No charge is taken from this page.</footer></div></KindleapShell>}
