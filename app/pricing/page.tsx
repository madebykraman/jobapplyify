import Link from 'next/link'
import { Check } from 'lucide-react'
import { BRAND } from '@/lib/brand'
import { Surface, SectionHeader, KindleapShell, Status } from '@/components/kindleap-ui'
const plans=[{name:'Free',price:'₹0',features:['LinkedIn PDF → one usable resume','Basic resume editing and export','Bring a job to the assistant','Human-reviewed application preparation'],href:'/assistant'},{name:'Pro',price:'Coming with checkout',features:['Multiple resume templates','Template selection and customisation','Higher-value application automation','Advanced assistant capabilities as released'],href:'/auth?mode=sign-up'}]
export default function Pricing(){return <KindleapShell><div className="kl-page"><SectionHeader eyebrow={BRAND.name+' / Access'} title="Useful first. Paid when the leverage is real." description="The free layer is deliberately useful. Pro is for meaningful customisation and convenience, not basic candidate dignity."/><div className="kl-card-grid">{plans.map(p=><Surface key={p.name}><div className="kl-row-head"><div><div className="kl-eyebrow">{p.name}</div><h2>{p.price}</h2></div>{p.name==='Pro'&&<Status>Pro</Status>}</div><div className="kl-checks">{p.features.map(f=><div key={f}><Check size={14}/>{f}</div>)}</div><Link className={'kl-button '+(p.name==='Pro'?'kl-button-primary':'')} href={p.href}>{p.name==='Pro'?'Create account':'Start free'}</Link></Surface>)}</div><footer className="kl-page-footer">Checkout and subscription lifecycle are not connected yet. No charge is taken from this page.</footer></div></KindleapShell>}
