import Link from 'next/link'
const stages=[['Preparing','4'],['Applied','12'],['Interview','3'],['Offer','1']]
export default function Applications(){return <main className="main standalone"><Link className="back" href="/">← Command</Link><div className="mono eyebrow">ROVA / APPLICATIONS</div><h1 className="title">The application pipeline.</h1><p className="lead">One durable place for application state, evidence and outcomes. Execution adapters connect later.</p><section className="pipeline">{stages.map(([name,count])=><div className="stage" key={name}><div className="stagehead"><span>{name}</span><strong>{count}</strong></div><div className="mini"><b>{name==='Applied'?'Linear':'Example role'}</b>{name==='Applied'?'Product Designer':'Application record'}</div></div>)}</section><section className="section card"><div className="mono">STATE MODEL</div><p className="lead">Saved → Preparing → Review → Applied → Interview → Offer → Closed</p></section></main>}
