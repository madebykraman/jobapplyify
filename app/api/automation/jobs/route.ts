import { NextResponse } from 'next/server'
import { supabaseForAccessToken } from '@/lib/supabase-server'
export const runtime='nodejs'
function auth(req:Request){const v=req.headers.get('authorization')||'';return v.startsWith('Bearer ')?v.slice(7):''}function validHttps(v:unknown){try{return new URL(String(v)).protocol==='https:'}catch{return false}}
export async function GET(req:Request){const client=supabaseForAccessToken(auth(req));if(!client)return NextResponse.json({error:'Authentication is required.'},{status:401});const{data:{user},error:ue}=await client.auth.getUser();if(ue||!user)return NextResponse.json({error:'Authentication is required.'},{status:401});const[{data:jobs,error},{data:profile}]=await Promise.all([client.from('automation_jobs').select('*').order('created_at',{ascending:false}).limit(200),client.from('profiles').select('automation_paused').eq('id',user.id).maybeSingle()]);if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({jobs:jobs||[],paused:Boolean(profile?.automation_paused)})}
export async function POST(req:Request){const client=supabaseForAccessToken(auth(req));if(!client)return NextResponse.json({error:'Authentication is required.'},{status:401});const{data:{user},error:ue}=await client.auth.getUser();if(ue||!user)return NextResponse.json({error:'Authentication is required.'},{status:401});try{const b=await req.json();if(!b?.applicationUrl||!validHttps(b.applicationUrl))return NextResponse.json({error:'A valid HTTPS application URL is required.'},{status:400});if(!['dry-run','review','full-auto'].includes(b.mode))return NextResponse.json({error:'Invalid automation mode.'},{status:400});const{data:p}=await client.from('profiles').select('automation_paused').eq('id',user.id).maybeSingle();if(p?.automation_paused)return NextResponse.json({error:'Automation queue is paused.'},{status:409});const{data,error}=await client.from('automation_jobs').insert({user_id:user.id,company:String(b.company||'').slice(0,200),role:String(b.role||'').slice(0,200),source:String(b.source||'').slice(0,100),application_url:b.applicationUrl,state:'queued',mode:b.mode,max_attempts:3,next_attempt_at:new Date().toISOString()}).select('*').single();if(error)return NextResponse.json({error:error.message},{status:500});await client.from('automation_events').insert({user_id:user.id,automation_job_id:data.id,event_type:'queued',state:'queued',message:'Automation job queued.'});return NextResponse.json({job:data},{status:201})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Invalid request.'},{status:400})}}
export async function PATCH(req:Request){const client=supabaseForAccessToken(auth(req));if(!client)return NextResponse.json({error:'Authentication is required.'},{status:401});const{data:{user},error:ue}=await client.auth.getUser();if(ue||!user)return NextResponse.json({error:'Authentication is required.'},{status:401});try{const b=await req.json();const action=String(b?.action||'');const jobId=String(b?.jobId||'');if(action==='pause'||action==='resume'){const paused=action==='pause';const{error}=await client.from('profiles').update({automation_paused:paused,updated_at:new Date().toISOString()}).eq('id',user.id);if(error)return NextResponse.json({error:error.message},{status:500});await client.from('automation_events').insert({user_id:user.id,automation_job_id:null,event_type:`queue:${action}`,state:paused?'paused':'running',message:`Automation queue ${paused?'paused':'resumed'}.`}).catch(()=>undefined);return NextResponse.json({paused})}if(!jobId||!['cancel','retry','follow-up'].includes(action))return NextResponse.json({error:'Invalid queue action.'},{status:400});const{data:job}=await client.from('automation_jobs').select('*').eq('id',jobId).single();if(!job)return NextResponse.json({error:'Automation job not found.'},{status:404});if(action==='cancel'){if(['submitted','verified','cancelled'].includes(job.state))return NextResponse.json({error:'Job cannot be cancelled in its current state.'},{status:409});const{error}=await client.from('automation_jobs').update({state:'cancelled',lease_owner:null,lease_expires_at:null,updated_at:new Date().toISOString()}).eq('id',jobId);if(error)return NextResponse.json({error:error.message},{status:500});await client.from('automation_events').insert({user_id:user.id,automation_job_id:jobId,event_type:'cancelled',state:'cancelled',message:'Automation job cancelled by the user.'});return NextResponse.json({state:'cancelled'})}if(action==='follow-up'){if(!['submitted','verified','human-review','ready'].includes(job.state))return NextResponse.json({error:'Follow-up is available after application preparation or submission.'},{status:409});const at=new Date(Date.now()+48*60*60*1000).toISOString();const{error}=await client.from('automation_jobs').update({follow_up_at:at,updated_at:new Date().toISOString()}).eq('id',jobId);if(error)return NextResponse.json({error:error.message},{status:500});await client.from('automation_events').insert({user_id:user.id,automation_job_id:jobId,event_type:'follow-up-scheduled',state:job.state,message:'Follow-up scheduled for 48 hours.',metadata:{followUpAt:at}});return NextResponse.json({followUpAt:at})}if(job.attempts>=job.max_attempts)return NextResponse.json({error:'Maximum attempts reached.'},{status:409});const{error}=await client.from('automation_jobs').update({state:'queued',handoff:null,last_error:null,worker_state:null,lease_owner:null,lease_expires_at:null,next_attempt_at:new Date().toISOString(),updated_at:new Date().toISOString()}).eq('id',jobId);if(error)return NextResponse.json({error:error.message},{status:500});await client.from('automation_events').insert({user_id:user.id,automation_job_id:jobId,event_type:'retry',state:'queued',message:'Automation job re-queued for another attempt.'});return NextResponse.json({state:'queued'})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Invalid queue action.'},{status:400})}}
