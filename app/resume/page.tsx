'use client'
import { useEffect, useMemo, useState } from 'react'
import Link from 'next/link'
import { FileText, Sparkles, Save, Download } from 'lucide-react'
import { analyzeResume, extractResumeText } from '@/lib/resume-engine'
import { defaultProfile, loadProfile, loadResumes, saveResumes, uid, type RovaProfile } from '@/lib/storage'
import { KindleapShell, SectionHeader, Signal, Surface } from '@/components/kindleap-ui'
const sampleRole=`Product Designer. Build and evolve design systems for a B2B SaaS product. Partner with product managers and engineers. Strong Figma, prototyping, UX research, interaction design, experimentation and accessibility skills. 3+ years experience.`
export default function ResumePage(){
 const [tab,setTab]=useState<'intelligence'|'builder'>('intelligence'); const [resume,setResume]=useState(''); const [role,setRole]=useState(sampleRole); const [busy,setBusy]=useState(false); const [name,setName]=useState('Resume'); const [profile,setProfile]=useState<RovaProfile>(defaultProfile); const [saved,setSaved]=useState(false)
 useEffect(()=>{setProfile(loadProfile());const first=loadResumes()[0];if(first){setResume(first.content);setRole(first.targetRole);setName(first.name)}},[])
 const analysis=useMemo(()=>analyzeResume(resume,role),[resume,role])
 async function file(e:React.ChangeEvent<HTMLInputElement>){const f=e.target.files?.[0];if(!f)return;setName(f.name.replace(/\.[^.]+$/,''));setBusy(true);try{if(f.type==='text/plain'||f.name.endsWith('.md'))setResume(extractResumeText(await f.text()));else if(f.name.endsWith('.docx')){const mammoth=await import('mammoth');const r=await mammoth.extractRawText({arrayBuffer:await f.arrayBuffer()});setResume(extractResumeText(r.value))}else if(f.type==='application/pdf'||f.name.endsWith('.pdf')){const pdfjs=await import('pdfjs-dist/legacy/build/pdf.mjs');const pdf=await (pdfjs.getDocument as any)({data:new Uint8Array(await f.arrayBuffer()),disableWorker:true}).promise;let out='';for(let i=1;i<=pdf.numPages;i++){const p=await pdf.getPage(i);const c=await p.getTextContent();out+=c.items.map((x:any)=>x.str).join(' ')+'\n'}setResume(extractResumeText(out))}else setResume(await f.text())}catch{setResume('Unable to parse this file in the current browser session. Paste the resume text below.')}finally{setBusy(false)}}
 function save(){const all=loadResumes();const record={id:uid('resume'),name,content:resume,targetRole:role,atsScore:analysis.score,updatedAt:new Date().toISOString()};saveResumes([record,...all.filter(x=>x.name!==name)]);setSaved(true);setTimeout(()=>setSaved(false),1800)}
 return <KindleapShell><div className="kl-page"><SectionHeader eyebrow="Resume / Evidence intelligence" title="Make the resume legible to the market." description="Import a resume, extract its evidence, compare it against a target role, then shape a clean version without inventing candidate facts."/><div className="kl-tabs" role="tablist"><button className={tab==='intelligence'?'active':''} onClick={()=>setTab('intelligence')}>Intelligence</button><button className={tab==='builder'?'active':''} onClick={()=>setTab('builder')}>Resume builder</button></div>{tab==='intelligence'?<section className="kl-split"><Surface><div className="kl-upload"><FileText size={22}/><div><b>{busy?'Parsing…':'Import resume'}</b><small>PDF, DOCX, TXT or Markdown</small></div><input type="file" accept=".pdf,.docx,.txt,.md" onChange={file}/></div><label className="kl-field"><span>Resume content · {name}</span><textarea value={resume} onChange={e=>setResume(e.target.value)} placeholder="Paste your resume text here…" rows={18}/></label><label className="kl-field"><span>Target job description</span><textarea value={role} onChange={e=>setRole(e.target.value)} rows={8}/></label><button className="kl-button kl-button-primary" onClick={save}><Save size={15}/>{saved?'Saved':'Save resume version'}</button></Surface><Surface className="kl-analysis"><div className="kl-eyebrow">SIGNAL / FIT CHECK</div><Signal label="Evidence fit" value={<>{analysis.score}<small>/100</small></>} detail={analysis.summary}/><AnalysisBlock title="Detected sections" items={analysis.sections}/><AnalysisBlock title="Matched terms" items={analysis.matchedSkills.slice(0,14)}/><AnalysisBlock title="Missing signal" items={analysis.missingSkills} muted/><div className="kl-evidence is-muted"><b>Checks</b>{analysis.warnings.length?analysis.warnings.map(x=><div key={x}>{x}</div>):<div>No structural warnings detected.</div>}</div><button className="kl-button" onClick={()=>setTab('builder')}><Sparkles size={15}/> Build targeted version</button></Surface></section>:<Builder profile={profile} resume={resume} setResume={setResume}/>}<footer className="kl-page-footer">Resume intelligence is evidence-bound. Unsupported qualifications, metrics and experience must never be introduced.</footer></div></KindleapShell>
}
function AnalysisBlock({title,items,muted=false}:{title:string,items:string[],muted?:boolean}){return <div className={`kl-evidence${muted?' is-muted':''}`}><b>{title}</b><div>{items.length?items.map(x=><span key={x}>{x}</span>):<small>None detected.</small>}</div></div>}
function Builder({profile,resume,setResume}:{profile:RovaProfile,resume:string,setResume:(v:string)=>void}){const generated=`${profile.name}\n${profile.headline}\n${profile.location} · ${profile.email} · ${profile.phone}\n\nSUMMARY\n${profile.headline || 'Add a concise professional summary based only on your verified experience.'}\n\nSKILLS\n${profile.skills.join(' · ') || 'Add verified skills in Profile.'}\n\nEXPERIENCE\n${resume.match(/(?:experience|work history)[\s\S]*/i)?.[0] || 'Paste or edit verified experience here.'}\n\nEDUCATION\n${resume.match(/education[\s\S]*/i)?.[0] || 'Add verified education here.'}`; const [value,setValue]=useState(resume||generated);return <section className="kl-split"><Surface><div className="kl-eyebrow">EDITABLE SOURCE</div><textarea className="kl-builder" value={value} onChange={e=>{setValue(e.target.value);setResume(e.target.value)}} rows={30}/><button className="kl-button kl-button-primary" onClick={()=>window.print()}><Download size={15}/> Print / save PDF</button></Surface><Surface><div className="kl-eyebrow">LIVE PREVIEW</div><pre className="kl-preview">{value}</pre></Surface></section>}
