'use client'
import { useState } from 'react'
import Link from 'next/link'
export default function AuthPage(){const [mode,setMode]=useState<'sign-in'|'sign-up'>('sign-in');const [email,setEmail]=useState('');const [message,setMessage]=useState('');function submit(e:React.FormEvent){e.preventDefault();setMessage('Local demo account ready. Connect Supabase to enable production authentication.')}return <main className="main standalone auth"><Link className="back" href="/">← Command</Link><div className="mono eyebrow">ROVA / ACCOUNT</div><h1 className="title">Your career workspace.</h1><p className="lead">Sign in to keep profile, documents and application state together.</p><section className="formcard"><div className="tabs"><button className={mode==='sign-in'?'selected':''} onClick={()=>setMode('sign-in')}>Sign in</button><button className={mode==='sign-up'?'selected':''} onClick={()=>setMode('sign-up')}>Create account</button></div><form onSubmit={submit}><label className="field"><span>Email</span><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label className="field"><span>Password</span><input type="password" required minLength={8} placeholder="8+ characters"/></label><button className="primary" type="submit">{mode==='sign-in'?'Sign in':'Create account'} →</button></form>{message&&<div className="notice">{message}</div>}<p className="small">Production auth is wired for Supabase. Until its project keys are configured, this surface remains local/demo only.</p></section></main>}
